## 3.0.37

### Features
- Add warning message for busy files

### Fixes
- Fix backup file check making crash if project does not exists anymore
- Fix deploy in web crashing if protect data activated
- Fix do not update map portions if not loaded yet
- Fix special elements lists that might crash if picture does not exists anymore
- Fix object 3D crash if it does not exists anymore in the project
- Fix avoid crash when copied item is incorrect + add missing models copy

### Other Changes
- When loading map, ensure temp file exists to avoid future crash issues

