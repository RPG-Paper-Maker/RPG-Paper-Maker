## 3.2.12

### Features
- Add display choices index storage

### Fixes
- Fix 3D object geometry memory leak in map editor
- Fix copy paste ID wrong for ligths
- Fix graphics character crash on selecting specific picture
- Fix multiple change screen tone command without wait end command to cancel previous progress and avoid weird behaviour
- Fix saved game with properties unchanged might always get 0 as value
- Fix create object from map / remove object from map command stock in local variable not working
- Fix detection orientation incorrect
- Fix send event stored last object ID inccorrect in some cases
- Fix jump to label command can be broken

### Other Changes
- Update move object jump command to have pixels selection possible for X/Z axis
- Selecting end if command row selects all condition command
- Update change property command error more clear

