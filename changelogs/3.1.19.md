## 3.1.19

### Features
- Add left/top increase in map properties
- Add buttons when no map selected to create new map/folder
- Add default values to variables
- Add categories for 3D objects
- Add grid for selecting mountain / walls / 3D objects / objects

### Fixes
- Fix status default messages
- Fix map properties dialog interactions with other UI tools
- Fix dialog map properties switching tabs makes undefined values in musics selectors
- Fix animations position center screen switch move animations elements to top left
- Fix edit map properties: do not change cursor position on save
- Fix unsaved data in animations for position, rows, columns
- Fix visuals disabling labels issues in command conditions others
- Fix try crash when trying to remove folder but not possible
- Fix unfocused test play and deploy games at start
- Fix status message undefined and not displayed correctly for new battle
- Fix animations not correctly positionned on window size != 720p
- Fix mountains footsteps sounds on big mountains
- Fix menu inventory incorrectly greying out some items
- Fix try music not played correctly with battle with no transition
- Fix collisions behavior to move characters correctly even with low FPS

### Other Changes
- Keep in mind if state options is open/closed in map object panel
- Preview new map/folder creation in list maps
- Improve dialog plugins selection
- Update default hero model to change graphics on leave main menu only if caterpillar activated
- Warning instead of crash when trying to export a file which path does not exists anymore
- Detect corrupted files to propose backups manager

