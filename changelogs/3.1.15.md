## 3.1.15

### Features
- Add command play video option to skip

### Fixes
- Fix try web issue loading locales fallback
- Fix Linux window size issue

### Other Changes
- Accept .mov for videos
- Change updater to be manual by default + copy main.js at every update without need of updating updater
- Update command menu enter name so if name is but empty, it keeps name as it was before

