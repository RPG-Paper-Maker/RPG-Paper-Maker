## 3.2.1

### Features
- Add option to disable live preview command

### Fixes
- Fix live preview commands crashes on flash screen / loop in some cases
- Fix try MacOS performances
- Fix GLTF not working for map objects + stop animation for GLTF in map editor
- Fix blank project not running game
- Fix try webGL context lost because of stop animation not disposing geometries
- Fix textures characters wrong selection on bigger square size
- Fix try web engine loading shaders sometimes failing
- Fix plugin cannot read data: do not catch error just ignore
- Fix GLTF object screen tone not applied
- Fix command change map properties crash after teleport hero to another map
- Fix try error with spawned object
- Fix changing screen size is making stretched pictures not stretched anymore

### Other Changes
- Improve layers rendering to have less spacing
- Impossible to move an object into another object position to remove it: should be removed manually before (secure)
- Clear collisions when changing square size in systems
- Use tar.gz instead of zip for every file related to electron dependencies for each os
- Improve shaders errors content to better debug
- Improve feedback when file busy by syn windows files/one drive etc, increase number of attempts
- Do not play intro video when you go back to title screen
- Remove loading full data game before showing title screen, silently download remaining data while in title screen

