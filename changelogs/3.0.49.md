## 3.0.49

### Fixes
- Fix cannot place sprites on walls if on borders south/east of map
- Fix dialog create parameter z index too low
- Fix dialog command call a common reaction height too small
- Fix new project with another language: correct language name instead of english
- Fix systems manager not saving frequencies changes
- Fix weird drag and drop with special elements / objects lists
- Fix too many errors toaster not sending the last error at the same time
- Fix 2.0 display choices are shown empty text in engine
- Fix special elements crashing if id does not exists
- Fix 3D objects not possible to remove
- Fix setting languages not applied after switching
- Fix try common reaction crashing in effects (parameters?)
- Fix several effects damage with death: do not loop on killing damages
- Fix ingame words appearing outside windows in menus
- Fix try icon cut off in item/skill description
- Fix loading shape issues if removed
- Fix camera target change issue with angle changes

### Other Changes
- When adding a new language, translate as much things as possible (based on default assets)
- Make default loading background color black
- Update undo redo system filenames
- Display some missing errors in updater
- Map portion outside visible ray portion is loaded in the background to improve fluid loading

